<!DOCTYPE html>
<html>

<head>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.3.15/angular-route.js"></script>
    <script src="js/ngapp.js"></script>
    <title>freestore.se </title>


    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body ng-app="freestore">
    <div class="col-md-offset-2 col-md-8">
        <div id="render" ng-controller="mainController">
            <form action="/search" method="post">
                Sök
                <input type="text" ng-model="search.searchText">
                <input type="submit" value="Sök efter {{search.searchText}}">
            </form>
        </div>


        <div ng-controller="ShowLatestItemsController">

            <div ng-repeat="thing in things">
                {{thing.title}} {{thing.time | date:'MM/dd/yyyy @h:mma'}}
                <img class="thumb" src="{{thing.photopath}}" />
            </div>
            <!--
            <div>
                <a href="/sak/{{things[0]._id}}">
                    <p> {{things[0].title}}</p>
                    <img class="thumb" src="{{things[0].photopath}}" />
                </a>
            </div>
            <div>
                <a href="/sak/{{things[1]._id}}">
                    <p> {{things[1].title}}</p>
                    <img class="thumb" src="{{things[1].photopath}}" />
                </a>
            </div>
            <div>
                <a href="/sak/{{things[2]._id}}">
                    <p> {{things[2].title}}</p>

                    <img class="thumb" src="{{things[2].photopath}}" />
                </a>
            </div>
        </div>
-->
            <a href="/nysak">Lagg till ny</a>

        </div>
</body>

</html>